-- Simple function definitions and type signatures
module Ghc005SimpleFunction where

add :: Int -> Int -> Int
add x y = x + y

multiply :: Int -> Int -> Int
multiply x y = x * y

constant :: String
constant = "hello"
