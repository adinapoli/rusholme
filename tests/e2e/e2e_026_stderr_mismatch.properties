# Intentional stderr mismatch: exercises the step-6 false branch in runTest.
# The binary produces empty stderr; the sidecar expects a non-empty string.
# xfail absorbs the expected failure; an [xpass] diagnostic would signal
# that mismatch detection has regressed.
xfail: intentional stderr mismatch to verify detection code path
