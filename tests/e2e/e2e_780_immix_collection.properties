# Test 780: Immix collection exercises shadow-stack roots
# Builds a 2000-element list under --rts=immix so the block budget
# overflows during the recursive build and auto-collect fires while
# live roots are on the stack. Validates that precise shadow-stack
# rooting keeps the partial list alive across collection points.
exit_code: 0
