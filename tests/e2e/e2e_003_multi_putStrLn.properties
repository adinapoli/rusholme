# Do-notation desugaring produces inline lambda expressions in GRIN IR that the
# LLVM backend cannot translate. The backend only handles top-level functions
# (from def.params) and doesn't support generating code for inline lambdas in
# function bodies. This needs either lambda lifting enhancement or inline lambda
# codegen support in the LLVM backend.
skip: Do-notation needs lambda lifting enhancement or inline lambda codegen support
