# E2E test for lazy function arguments (#517)
#
# This test verifies that function arguments are not eagerly evaluated.
# Without lazy arguments, `error "unused"` would be evaluated when calling
# `const "hello" (error "unused")`, causing a runtime error.
#
# tracked in: https://github.com/adinapoli/rusholme/issues/545
skip: native compiler thunk forcing not yet implemented (#545)
