# Test 327: PrimOp/GC integration
# Forces auto-collect to fire mid-PrimOp under --rts=immix. Asserts
# that comparison/arithmetic PrimOps preserve their arguments and
# results across collection by going through the shadow-stack roots
# (#780) the caller already pushed at each *Node SSA def.
exit_code: 0
